use kts::ast::AST_GRAPH;
use kts::ast::{ast_node::stat::Stat, ASTNode};
use visulize::Vis;

extern crate kts;

// #[macro_use]
// extern crate visulize;

// use kts::ast::visulize::Visualizable;

// #[allow(dead_code, unused_imports)]
// #[derive(Vis)]
// struct ABC {
//     s2: Vec<ASTNode<Stat>>,
//     s3: ASTNode<Stat>,
//     s4: Option<ASTNode<Stat>>,
// }

#[cfg(test)]
mod test {
    #[test]
    fn test() {}
}
